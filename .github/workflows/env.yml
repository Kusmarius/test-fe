name: export test

on:
  workflow_dispatch:

jobs:
  redis-job:
    runs-on: ubuntu-latest
    environment: develop

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: echo
        run: |
          echo "Redis: $REDIS_HOST"

